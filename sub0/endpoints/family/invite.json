{
  "id": "family-invite-01",
  "resource": "family/invite",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "sql_query": {
        "query": "INSERT INTO _family_member (id, userId, careRecipientId, role, permissions, invitedAt, createdAt, updatedAt) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) ON CONFLICT (userId, careRecipientId) DO UPDATE SET role = $4, permissions = $5, updatedAt = $8",
        "parameters": ["$GENERATE_UUID", "userId", "careRecipientId", "role", "permissions", "$DATETIME", "$DATETIME", "$DATETIME"]
      }
    }
  ],
  "returnables": ["*"]
}
