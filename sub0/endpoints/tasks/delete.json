{
  "id": "tasks-delete-01",
  "resource": "tasks/:id/delete",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "sql_query": {
        "query": "DELETE FROM _care_task WHERE id = $1 AND careRecipientId IN (SELECT id FROM _care_recipient WHERE primaryCaregiverId = $2)",
        "parameters": ["$PARAM.id", "$PROTECTED.id"]
      }
    }
  ],
  "returnables": ["id"]
}
