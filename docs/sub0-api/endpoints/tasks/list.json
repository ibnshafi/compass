{
  "id": "tasks-list-01",
  "resource": "tasks",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "sql_query": {
        "query": "SELECT t.*, cr.firstName as recipientFirstName, cr.lastName as recipientLastName FROM _care_task t JOIN _care_recipient cr ON t.careRecipientId = cr.id WHERE cr.primaryCaregiverId = $1 ORDER BY t.createdAt DESC",
        "parameters": ["$PROTECTED.id"]
      }
    }
  ],
  "returnables": ["*"]
}
