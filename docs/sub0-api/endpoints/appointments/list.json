{
  "id": "appointments-list-01",
  "resource": "appointments",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "sql_query": {
        "query": "SELECT a.*, cr.firstName as recipientFirstName, cr.lastName as recipientLastName FROM _appointment a JOIN _care_recipient cr ON a.careRecipientId = cr.id WHERE cr.primaryCaregiverId = $1 ORDER BY a.dateTime DESC",
        "parameters": ["$PROTECTED.id"]
      }
    }
  ],
  "returnables": ["*"]
}
